# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 👇 Instalamos dependencias en la carpeta del frontend
      - run: npm ci
        working-directory: frontend/app

      # 👇 Ejecutamos el build en la carpeta del frontend
      - run: npm run build
        working-directory: frontend/app

      # 👇 Deploy desde la carpeta donde está firebase.json
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HOSPEDAJE_REACT_CLINICA }}
          channelId: live
          projectId: hospedaje-react-clinica
          entryPoint: frontend/app
